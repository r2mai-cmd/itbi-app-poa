package com.example.itbipoa.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta baseada no template aprovado (fundo azul-petróleo + cards em creme),
// com os tons extraídos da ilustração da Usina do Gasômetro ao pôr do sol.

val Pergaminho = Color(0xFFECE6D3)          // creme dos cartões (extraído do mockup)
val PergaminhoEscuro = Color(0xFFE2DBC5)    // variação um pouco mais escura, se precisar de contraste
val TintaVerde = Color(0xFF1F3B33)          // texto principal sobre os cartões (creme)
val TintaVerdeClara = Color(0xFF3C5B50)     // texto secundário sobre os cartões
val Terracota = Color(0xFFC96A44)           // tijolo da usina — destaque / ação principal
val TerracotaClara = Color(0xFFE0916B)
val DouradoLinha = Color(0xFFC9B78C)        // linhas divisórias, sutis
val VermelhoErro = Color(0xFFA3372A)

// Fundo principal do app — azul-petróleo extraído do template (a "água" por
// trás dos cartões flutuantes), e as cores extraídas da ilustração ao pôr do
// sol (céu, água e o brilho quente do horizonte):
val FundoOceano = Color(0xFF1B5876)         // fundo principal do app (fora dos cartões)
val CeuAzul = Color(0xFF21386E)             // azul-noite do céu no topo da ilustração nova
val CeuAzulProfundo = Color(0xFF16294F)     // variação mais escura, para a faixa da tela de detalhe
val AguaTurquesa = Color(0xFF79D5D2)        // água clara do Guaíba
val AguaProfunda = Color(0xFF13697A)        // água escura / sombra do rio
val MataVerde = Color(0xFF4C642E)           // folhagem em primeiro plano

// Texto usado diretamente sobre o fundo azul-petróleo (fora dos cartões).
val TextoSobreFundo = Color(0xFFF3EEE0)
val TextoSobreFundoSutil = Color(0xB3F3EEE0) // mesma cor, com transparência p/ texto secundário

val NoiteVerde = Color(0xFF10201C)          // fundo modo escuro
val NoiteSuperficie = Color(0xFF172B25)
val PergaminhoSobreNoite = Color(0xFFEDE6D6)


