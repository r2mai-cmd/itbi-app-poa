package com.example.itbipoa.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta "livro de registro": pergaminho + tinta verde-escura + terracota.
// Deliberadamente fora do azul/roxo padrão do Material, para não parecer
// um app genérico gerado a partir do tema default.

val Pergaminho = Color(0xFFF6F1E4)          // fundo principal, quente
val PergaminhoEscuro = Color(0xFFEDE5D3)    // superfícies elevadas (cards/linhas)
val TintaVerde = Color(0xFF1F3B33)          // texto principal / primária
val TintaVerdeClara = Color(0xFF3C5B50)
val Terracota = Color(0xFFB5502E)           // destaque / ação principal
val TerracotaClara = Color(0xFFD98860)
val DouradoLinha = Color(0xFFC9B78C)        // linhas divisórias, sutis
val VermelhoErro = Color(0xFFA3372A)

val NoiteVerde = Color(0xFF10201C)          // fundo modo escuro
val NoiteSuperficie = Color(0xFF172B25)
val PergaminhoSobreNoite = Color(0xFFEDE6D6)
